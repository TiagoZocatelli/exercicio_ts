function soma(a:number, b:number): number {
    const mult = a * b;
    return mult;
}

console.log(soma(10,50))

function dizOla(nome: string): string {
    return `Olá ${nome}`;
}

const seuNome = "Tiago";
console.log(dizOla(seuNome));